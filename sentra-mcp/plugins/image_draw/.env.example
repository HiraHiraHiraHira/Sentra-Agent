# 是否启用插件
PLUGIN_ENABLED=true

# 插件超时时间，毫秒
PLUGIN_TIMEOUT_MS=150000

# 绘图模型
DRAW_MODEL=luminaArchitect-v1-plus

# 绘图服务地址
DRAW_BASE_URL=https://yuanplus.chat/v1

# 绘图服务密钥
DRAW_API_KEY=sk-REPLACE_ME

# 调用模式：chat 或 images（默认 chat）chat 走 /v1/chat/completions，让模型返回 Markdown 图片链接。images 走 /v1/images/generations，直接生成图片。
DRAW_MODE=chat

# 当 DRAW_MODE=images 时生效，OpenAI 兼容尺寸，如 512x512 / 1024x1024 / 1024x1024。
DRAW_IMAGE_SIZE=1536x1024
