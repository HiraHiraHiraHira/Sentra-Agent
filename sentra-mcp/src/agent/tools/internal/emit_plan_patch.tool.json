{
  "type": "function",
  "function": {
    "name": "emit_plan_patch",
    "description": "Decide whether to patch remaining plan steps and output patch operations (append/replace/delete) using stepId as the stable identifier.",
    "parameters": {
      "$ref": "./emit_plan_patch.schema.json"
    }
  }
}
